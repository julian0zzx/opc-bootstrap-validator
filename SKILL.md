---
name: opc-bootstrap-validator
display_name: OPC 启动验证器
description: 为一人公司（OPC）创始人的创业方向做结构化的自查与最小验证判定。第一层按五道通用闸门（生存与止损 / 能力与资源 / 问题与客户 / 付费与交付 / 单位经济）逐级核对证据，硬红线命中即锁定，无证据的自我评分由代码封顶 2 分，伪验证信号（上线、涨粉、阅读量、订单、销售额）被拦截并强制替换为可验证指标。第二层按「主要收入来源 + 主要风险来源」把方向归入 8 类 / 69 种模式并匹配其专属指标包，产出「北极星 1 + 过程 2 + 安全 2」五槽位合并看板与继续/调整/暂停决策；两层是 AND 关系。产出中性的 HTML 决策书，可用于自用或对外交付。当用户想做创业方向自查、判断能不能辞职全职、核算单客经济、判别「有人感兴趣」是否等于已验证、询问 OPC 适配度评分、问「我这算什么模式」「该盯哪些指标」或「这个月该继续还是调整」时使用。
category: business
version: 0.6.1
author: julian0zzx （振兴）
tags: [一人公司, OPC, 创业自查, 最小验证, 单位经济, 模式指标, 决策书]
license: AGPL-3.0
agent_created: true
---

# OPC 启动验证器

把《OPC 单人创业自查与最小验证流程》的表格判据固化为确定性计算，回答两个问题：

1. **这个方向能不能开始？**（第一层 · 五道通用闸门）
2. 这种模式是否出现了该模式应有的真实增长信号？（第二层 · 模式专属指标）

当前版本 v0.6.1，两层闸门已全部实现：第一层五道通用闸门，第二层模式专属指标与合并看板。

第一层按五道闸门逐级 AND 判定：任何一道闸门缺证据，结论都会被压到 `CONDITIONAL` 以下。评分只能往下压，不能往上抬。

| 闸门 | 回答的问题 | 命令 |
|---|---|---|
| 闸门 1 · 生存与合规 | 输得起吗？有没有一票否决的硬红线？ | `gate1` |
| 闸门 2 · 能力与资源 | 这个人现在具备做这件事的条件吗？ | `score` |
| 闸门 3 · 问题与客户 | 方向选对了吗？客户真的痛吗、真的碰得到吗？ | `direct` + `funnel` |
| 闸门 4 · 付费与交付 | Offer 卖得掉吗？单人交付得住吗？ | `mso` + `delivery` |
| 闸门 5 · 单位经济 | 这单到底赚不赚钱？ | `econ` |
| 全程 | 有没有在用伪验证信号自我说服？ | `signal` / `scan` |
| 止损 | 什么时候该停、该转向、该退出？ | `stop` |
| 第二层 · 模式专属指标 | 这种模式该看哪些真实增长信号？ | `modes` + `board` |

两层是 AND 关系。第二层数据再漂亮，第一层任一闸门不过（尤其硬红线）也只能输出"仅副业"；第一层过了但第二层没出现该模式应有的信号，同样不构成扩大投入依据。

## 一、触发场景

- "我想辞职做 XX，帮我判断一下能不能行"
- "做个创业自查 / 用五道闸门帮我过一遍"
- "有人说感兴趣，这算不算验证过了？"
- "帮我算一下这个单子到底赚不赚钱"
- "帮我做 OPC 适配度评分"
- "我这算什么模式？该盯哪些指标？"（第二层：`modes`）
- "帮我做一张每周经营看板 / 这个月该继续还是调整？"（第二层：`board`）

## 二、职责分工

| 角色 | 职责 |
|---|---|
| 宿主 Agent（本文件） | 按闸门顺序提问、收集证据、组装输入 JSON、调引擎、交付 HTML 决策书 |
| `scripts/gate.py` | 只做判据计算与渲染，标准库零依赖，不做问答、不做猜测 |
| `assets/modes.json` | 第二层数据源：8 类 / 69 模式 / 29 指标包 / 五槽位 / 决策表（**唯一事实来源**） |

## 三、引擎命令

```bash
cd ~/.workbuddy/skills/opc-bootstrap-validator
python3 scripts/gate.py <command> [args]
```

| 命令 | 用途 |
|---|---|
| `gate1 --config c.json` | 闸门 1：6 条硬红线、7 条软风险、止损三数字、安全垫月数 |
| `score --config c.json` | 闸门 2：16 项加权评分（满分 100），无证据项封顶 2 分 |
| `direct --config c.json` | 闸门 3：方向生成校验（四类证据）+ 9 项方向评分卡 + 命名体检 |
| `funnel --config c.json` | 闸门 3：预验证门槛（访谈 / 愿进一步 / 付款）+ 八环节漏斗与瓶颈 |
| `mso --config c.json` | 闸门 4：一页纸启动书 19 字段 + MSO 最低标准 7 条 |
| `delivery --config c.json` | 闸门 4：首 3 个客户的 7 条验证标准 |
| `econ --config c.json` | 闸门 5：单客贡献利润、贡献利润率、有效时薪、获客回本周期 |
| `stop --config c.json` | 止损与退出监控：立即停止 / 建议停止 / 两轮无付款 / 超限，含调整顺序与替代路径 |
| `signal --text "..."` | 伪验证信号拦截（第十二章 12.5 五类）+ 信号强度分级 |
| `scan --config c.json` | 批量扫描多段进展描述里的伪验证信号 |
| `assay --config c.json` | **第一层合成结论**（主命令，五道闸门 AND） |
| `state --config c.json [--stage S] [--note "…"] [--layer2 …] [--print]` | 读写状态档案，跨会话续跑 |
| `report --config c.json --out 报告.html [--mask\|--no-mask] [--sync-state] [--sync-only]` | 生成 HTML 决策书（默认对外脱敏）；`--sync-state` 出报告同时写状态档案，`--sync-only` 只写档案不出报告 |
| `modes --config c.json` | 第二层：模式归类（8 类 / 69 模式）+ 取出 29 个指标包之一，含叠加包 |
| `board --config c.json [--out 看板.html]` | 第二层：合并看板（五槽位 + 通用 6 类 + 9.2 决策 + 记录格式） |
| `template --out c.json` | 生成空白输入模板 |
| `selftest` | 判据回归自检（45 项：4 项 HTML 渲染 + 1 项全库版本一致性 + 1 项 frontmatter 结构 + 1 项提问节奏约束，幂等） |
| `version` | 版本信息 |

**退出码**：`0` 成功 · `1` 自检有失败用例 · `2` 输入错误 · `4` 第二层被顺序闸门拒绝（没有合格候选方向）。

输入路径的两种写法都接受：`funnel` / `mso` / `delivery` 既可写成顶层键，也可按章节语义嵌在 `gate3.funnel` / `gate4.mso` 下（顶层优先）。第二层数据统一放在 `layer2` 下。

## 四、执行流程（宿主照此执行）

### 4.0 提问节奏（硬约束，优先级等同于闸门顺序）

下面 7 条对**所有批次**生效。违反它们的后果比漏问一道题严重：用户没答完就放弃，这一步就白做了。

| # | 规则 | 说明 |
|---|---|---|
| R1 | **单轮上限 3 问** | 一条消息里最多 3 个需要用户作答的问题，第 4 个必须留到下一轮。**例外**：同一维度、只需报一个 1–5 分或一个数字、不需要翻凭证的同质题，可以 4 项一轮；涉及回忆案例、凭证、原话的开放题，一轮最多 2 个。 |
| R2 | **一题一行** | 每个问题一行字，用户能一句话答完。禁止把一批问题铺成大表格；确需展示选项时，表格**不得超过 3 行**，锚定示例每轮最多给 2 条。 |
| R3 | **无快车道** | 不提供"一次把全部问题给我"的选项。用户这样要求时回：这套流程靠一轮一轮的答案判断，一次铺完你反而答不准，想快就一轮多答几条。 |
| R4 | **"没有"是完整答案** | 用户答"没有 / 不知道 / 跳过"，宿主立刻记空并标注，**不追问、不说教、不解释方法论**，直接进下一批。缺证据本身就是结论。 |
| R5 | **每轮收尾一句进度** | 每批答完回一句 `闸门 X · 第 a/b 批完成 · 下一批：<主题>`，让用户知道还剩多少，不要让他觉得没头。 |
| R6 | **复述确认（严格限条件）** | 宿主可以把用户**本轮已说过的事实**复述成一个判断请他确认，格式「我按你说的理解成 X，对吗？」。四条边界：① 只能引用用户本轮原话，不得引入画像或历史会话；② 必须显式确认才作数；③ 用户改口或不予确认 → 按未评估处理，封顶 2 分；④ **禁止**复述出用户没说过的证据（付款、工时、数字、日期）。复述是为了省轮次，不是用来补证据。 |
| R7 | **用户超前作答要接住** | 用户一次答了后面几批的内容，宿主接受并跳过已被覆盖的批次，**不重复问**。这不是快车道——快车道由用户自己发起，宿主不得主动提供。 |

**「清单式」问题**：二值 / 命中式条目（硬红线 6 条、软风险 7 条、首 3 单 7 条这类"有没有 / 是不是"）可以合成 **1 问**：

> 以下 N 条，把你命中的报编号；都没有就回"都没有"。

这是 1 问，不计 N 问。**涉及数字、案例、凭证的条目不得清单化**——那类必须逐条问。

**批次总表**（宿主照此推进）

| 闸门 | 批次 | 每轮题量 | 轮数 |
|---|---|---|---|
| 1 | 1-1 安全垫三数 / 1-2 硬红线 6 条（清单式）/ 1-3 软风险 7 条（清单式）/ 1-4 止损三数字 | 3 / 1 / 1 / 3 | 4 |
| 2 | 按权重由高到低：10 分 2 项 / 8 分 2 项 / 6 分 2 项（能力闭环）/ 6 分 2 项（资源与风险）/ 市场证据 2 + OPC 结构 2 / 动机与耐久度 4（同质自评） | 2 / 2 / 2 / 2 / 4 / 4 | 6 |
| 3 | 3-1 方向证据 / 3-2 市场缺口 + 海外触达 / 3-3 复述候选方向待确认 / 3-4 各方向硬约束（3 行小表）/ 3-5 晋级方向其余评分 / 3-6 漏斗数字 | 3 / 2 / 0 / 1 / 逐方向 ≤3 / 1 | 6–7 |
| 4 | 4-1…4-6 MSO 19 字段（每轮 ≤3 字段）/ 4-7 首 3 单 7 条（清单式）/ 4-8 试跑跟踪 | 3 × 6 / 1 / 1 | 8 |
| 5 | 单位经济 | ≤3 | 1 |
| 第二层 | 8-1 收入来源 + 主要风险 / 8-2 北极星 / 8-3 过程 / 8-4 安全 / 8-5 看板数值 | 2 / 1 / 2 / 2 / 1 | 5 |

第一层 25–27 轮、第二层 5 轮。用户多处答"没有"或超前作答会明显少于这个数——**少问不等于漏判**，缺的项引擎会标出来。

可以整块留空的批次（评分项、MSO 字段、漏斗数字），开问前加一句：

> 这一批你要是都没有，直接说"都没有"，我会标成待填，不追问。

**跨会话续跑**：每过一个闸门跑一次 `state --config c.json --note "闸门X·批y 完成"`。下次开场先 `state --config c.json --print` 读回批次指针与已有档案，从下一批继续，不要从第 1 批重问。

### 第 0 步：开场

说明流程：先过五道闸门，**约 20 轮、每轮 2–3 个问题**，最后给一份 HTML 决策书。任何一批都可以说"跳过"或"没有"。开场就要讲明：这份流程不会给出"可以辞职"的结论，它的作用是找出还缺哪些证据。

开场必须先声明：本次只依据你现在说的话判定，不引用你的历史会话与记忆画像。如果你想拿以前做过的某个项目来验证，请自己明确说出来，宿主不得替用户"翻出来"。

### 第 1 步：闸门 1（4 轮）

| 批 | 问什么 | 题量 |
|---|---|---|
| 1-1 | 可动用现金 / 每月最低必要支出 / 目标安全垫月数 —— 数字题，一行一问 | 3 |
| 1-2 | 6 条硬红线：清单式列出，报编号；都没有就回"都没有" | 1 |
| 1-3 | 7 条软风险：同上 | 1 |
| 1-4 | 最大现金投入 / 最长试跑时间 / 最低继续结果 —— 三项缺任意一项引擎判 `INCOMPLETE` | 3 |

产出入参：`gate1.safety_cushion`（可动用现金、每月最低必要支出、目标月数）· `gate1.redlines.*`（6 个布尔值）· `gate1.soft_risks[]`（7 个键里选命中的）· `gate1.stop_loss`。

硬红线**不要**合并成"有没有风险"这种模糊问题，但可以按清单一次列出让用户挑。命中任一条就当场告知锁定，不要继续往下问评分，跳到第 7 步只出前半份报告。

### 第 2 步：闸门 2（6 轮，逐项要证据）

16 项按权重由高到低分 6 批，每批 ≤2 项（动机与耐久度那 4 项是同质自评，合成一批）。评分锚定见 `references/证据分级与锚定表.md`。

每项固定三句话：① 锚定（1 分的样子 / 5 分的样子）；② "有什么具体案例、记录、回款或凭证能对得上？"；③ 用户已说过的部分走 R6 复述确认。

> 用户说"我觉得我可以"→ 追问"有没有发生过一次？什么时候？结果是什么？"仍然没有 → `evidence` 留空，引擎自动封顶 2 分，**不纠缠**。
> 用户明确说不确定 / 不想答 → 该项不填，引擎按"未评估"处理并单独标记。

### 第 3 步：闸门 3（6–7 轮）

顺序不能颠倒：先出方向，再谈客户。

| 批 | 问什么 | 题量 |
|---|---|---|
| 3-1 | 过去 3–5 年反复做成的事 / 别人反复来问的事 / 14 天能交付且说得清验收标准的事 | 3 |
| 3-2 | 客户已在花钱解决但现有方案做不好的问题 / 海外触达的真实经历（没有就说没有） | 2 |
| 3-3 | 宿主据 3-1、3-2 **复述**出 3–5 个候选方向，请用户删改（走 R6） | 0 |
| 3-4 | 各候选方向的 3 项硬约束用一张 **3 行小表**一次性给分，每方向一行 | 1 |
| 3-5 | 只对**通过硬约束**的方向问其余 6 项，逐方向推进 | ≤3 / 方向 |
| 3-6 | 漏斗 8 个数字一起给，没有的答"还没有" | 1 |

候选方向必须写成"客户人群 + 场景 + 结果 + 交付形式"。不合格的例子："AI 咨询" / "代运营" / "内容创作"，引擎会标"命名待改"。每个方向记 `basis`：`past_results` / `referrals` / `market_gap` / `fast_deliverable`。

- `gate3.directions[]`：每个方向填 `name` / `basis` / `scores`（9 项）
- `gate3.funnel`：`targets` / `reached` / `interviews` / `advanced` / `proposals` / `quotes` / `payers` / `payments` / `deliveries` / `results`
- `gate3.funnel.loss_reasons{}`：收不到款时按 6 类归因，不得直接得出"市场不存在"（容错：写成上层 `gate3.loss_reasons` 或顶层 `loss_reasons` 也读得到）

先跑 `direct` 看有没有合格方向。三项硬约束（合规与依赖风险 / 客户可触达性 / 交付可控性）任一项低于 3 分，该方向即不合格，必须换方向或改交付范围，不要靠其他项的高分补救。

再跑 `funnel`。没有真实付款时结论是 `NO_PAYMENT`，表示还没到能下结论的时候，要归因到具体环节。

### 第 4 步：闸门 4（8 轮）

一页纸 Offer 的 19 个字段**分 6 轮问完，每轮 ≤3 个字段**。用户前面已经说过的，走 R6 复述确认，不重复问。

- 4-1 … 4-6：`gate4.mso.*` 19 字段，第 4 条必须收费，价格为 0 即不通过
- 4-7（清单式 1 问）：`gate4.delivery.*` —— `paying_customers` / `scope_controlled` / `econ_positive` / `feedback` / `follow_ons` / `compliant` / `still_willing` 7 条
- 4-8：`tracking` —— 现金已投入 / 上限、已试跑周数 / 上限，加上本轮改变了哪 1–2 个变量、有没有收到钱

出现绝对化表述（保证 / 确保 / 一定能 / 100% / 零风险…）会被直接判第 6 条不通过，必须改成"客户结果描述"。

每一轮验证只能改变 1–2 个关键变量；最多 2 轮（约 60 天）。两轮仍无付款记录，`stop` 会给出 `EXIT` 并列出替代路径。

### 第 5 步：闸门 5 前置（单位经济，1 轮）

问是否有真实订单。有 → 收集 `revenue` / `direct_cost` / `hours`，连同目标时薪、获客成本（`cac`）与单客月贡献利润（用于算回本周期），**每轮不超过 3 个数字**。
没有订单也不算失败：留空即可，引擎输出 `NO_DATA` 且不阻塞（文档允许早期无交易）。

### 第 6 步：信号真实性检查（不单独占轮）

用 `signal --text "…"`（单段）或 `scan --config c.json`（多段，扫描输入的 `signal_texts[]` 字符串数组）扫用户已答内容里的伪验证表述。命中就必须当场纠正：把"上线了 / 有 1000 粉丝 / 阅读量不错 / 有订单 / 有销售额"替换成对应右列指标，再问一次真实数字。**这一步只在用户自己提到这类说法时触发，不主动开一批。**

### 第 7 步：合成、出报告、存档

```bash
python3 scripts/gate.py assay  --config opc_input.json
python3 scripts/gate.py report --config opc_input.json --out OPC决策书.html --sync-state
```

把 HTML 交付给用户。结论不是 `READY_FOR_LAYER2` 时，不要提议进入第二层。

### 第 8 步：第二层（5 轮，仅在第一层放行后）

| 批 | 问什么 | 题量 |
|---|---|---|
| 8-1 | 「你的钱主要从哪来？」+「你最担心哪一类风险？」 | 2 |
| 8-2 | 北极星指标 1 个（候选池由引擎给出） | 1 |
| 8-3 | 过程指标 2 个 | 2 |
| 8-4 | 安全指标 2 个 | 2 |
| 8-5 | 五槽位 + 通用 6 类看板的数值，用户原样填写，缺失标「待填」 | 1 |

第一模式由主要收入来源（+ 主要风险来源）决定，方向名称只是弱信号。

```bash
python3 scripts/gate.py modes --config opc_input.json
python3 scripts/gate.py board --config opc_input.json --out OPC第二层看板.html
python3 scripts/gate.py report --config opc_input.json --out OPC决策书.html   # 配置有 layer2 时自动追加 §11/§12
```

- 只给了方向名 → 引擎返回 `NEEDS_INPUT` 并回问收入来源，不要自己替用户挑一个模式
- 归类后从该模式的指标包里挑 北极星 1 + 过程 2 + 安全 2，候选池由引擎给出，**用户来选**
- 五槽位、通用 6 类看板、记录格式 11 项的数值必须用户原样填写，缺失标「待填」
- 看板顶部永远回显第一层状态；第一层未通过时明示「不构成扩大投入依据」

## 五、判定档位（`assay` 输出）

| 档位 | 含义 | 宿主行为 |
|---|---|---|
| `LOCKED` | 硬红线命中 | 只谈低成本副业测试；评分结果不再展示为主结论 |
| `INCOMPLETE` | 止损数字或评分证据缺失 | 要求补齐，不输出适配度结论 |
| `INSUFFICIENT` | 适配度 < 60，或闸门 3 无合格方向，或止损已触发 | 给短板修复清单 |
| `CONDITIONAL` | 60–79 分、软风险 ≥3 项、单位经济为负、闸门 3/4 未评估或尚无真实付款、MSO 不可售 | 给短板修复清单，明确"修复期内不全职启动" |
| `READY_FOR_LAYER2` | 无红线、软风险 <3、适配度 ≥80，且 gate3 有合格方向 + 漏斗达标 + MSO 可售 | 可进入第二层模式指标自查 |

评分只能往下压。适配度 80 分但漏斗里付款人数为 0，结论就是 `CONDITIONAL`；报告里会单列一张「被压制的档位」表说明是哪条阻断项把它压下来的，这张表不允许被隐藏或改写。

最高档的含义是"可进入第二层"，离"可以去辞职"还差得远。完整判据：真实付款 + 可控交付 + 单位经济为正 + 风险可控同时出现（`scale_gate`），且两层闸门同时通过，才谈扩大投入（第十一章 / 第十二章）。

## 六、输入 JSON 结构

用 `python3 scripts/gate.py template --out opc_input.json` 生成骨架，结构见 `assets/input_schema.json`。

## 七、判据与章节对照

| 引擎常量 / 命令 | 出处 |
|---|---|
| `HARD_REDLINES`（6 条） | 第二章 2.1 |
| `SOFT_RISKS`（7 条） | 第二章 2.2 |
| 安全垫月数公式 | 第二章 2.1 末段 |
| `STOP_IMMEDIATE`（3 条 / 立即停止） | 第十章 10.1 |
| `STOP_SUGGEST`（6 条 / 命中 2 条即建议停止） | 第十章 10.2 |
| `ALT_PATHS`（6 条替代路径） | 第十章 10.3 |
| `SCORE_ITEMS`（16 项 / 满分 100） | 第三章 3.2 |
| `NO_EVIDENCE_CAP = 2` | 第三章 3.1 |
| 分档 80 / 60 | 第三章 3.3 |
| `SOFT_RISK_OVERLAY = 3` | 第三章 3.3 叠加规则 |
| `REPAIR_MAP`（16 项短板修复） | 第三章 3.4 |
| `DIRECTION_BASES`（四类证据来源） | 第四章 4.1 |
| `DIRECTION_ITEMS`（9 项 / 权重合计 100%） | 第四章 4.2 |
| `DIRECTION_HARD_FLOOR` + `DIRECTION_FLOOR = 3` | 第四章 4.2 三项硬约束 |
| `PREVALIDATION`（访谈 10 / 愿进一步 3 / 付款 1） | 第五章 5.2 |
| `FUNNEL_STAGES`（八环节） | 第五章 5.3 |
| `LOSS_REASONS`（6 类收不到款的归因） | 第五章 5.2 |
| `MSO_FIELDS`（19 字段） | 第六章 6.1 |
| `ABSOLUTE_CLAIMS`（绝对化表述） | 第六章 6.2 第 6 条 |
| `CONTRACT_TERMS`（7 项书面约定） | 第六章 6.2 第 7 条 |
| `judge_delivery`（首 3 个客户 7 条） | 第七章 7.2 |
| `ADJUST_ORDER`（调整顺序）+ `MAX_ROUNDS = 2` / `ROUND_DAYS = 30` | 第七章 7.3 |
| `judge_econ` 拒答规则 + `payback_periods` | 第六章 6.3 |
| `scale_gate`（扩大投入四条件） | 第十一章末段 |
| `PSEUDO_SIGNALS`（5 类） | 第十二章 12.5 |
| `SIGNAL_LEVELS`（4 级） | 第一章末段 / 第五章 5.2 |
| `ASSAY_LEVELS` 语义 | 第九章 9.2 / 第十章 10.2 / 第十二章末 |
| `categories`（8 类 / 69 模式） | 第十二章 12.2 |
| `metric_packs`（29 个）+ `mode_to_pack` | 第十二章 12.3 |
| `board_slots`（1 + 2 + 2）+ `record_fields`（11 项） | 第十二章 12.4 |
| `board_general`（通用 6 类周看板） | 第九章 9.1 |
| `decision_table`（继续 / 调整 / 暂停） | 第九章 9.2 |

全文见 `references/方法论全文.md`（收录完整，未删改）；第二层的可读索引与用法见 `references/第二层模式与看板.md`。

## 八、边界处理

| 情况 | 处理 |
|---|---|
| 硬红线命中 | 立即锁定，不再走评分流程；报告只出前半份 |
| 止损三数字缺项 | `INCOMPLETE`，不输出适配度结论 |
| 评分项无证据 | 代码封顶 2 分，报告中标注「封顶」 |
| 评分项未评估 | 按无证据封顶 2 分并单独标「未评估」 |
| 软风险命中 ≥3 | 即使 ≥80 分也降级为 `CONDITIONAL` |
| 闸门 3 无合格方向 | `INSUFFICIENT`；三项硬约束低于 3 分的方向不进推荐位 |
| 闸门 3 漏斗无付款 | `CONDITIONAL`（`NO_PAYMENT`），归因到具体环节而非"市场不存在" |
| 闸门 3/4 未评估 | 即使适配度 90 分也压到 `CONDITIONAL` |
| MSO 价格为 0 或免费 | 第 4 条不通过，不得视为已验证 |
| MSO 出现绝对化表述 | 第 6 条不通过；要求改为客户结果描述 |
| 首 3 单交付部分满足 | 不阻塞结论，但 `scale_gate` 的"可控交付"不通过 |
| 两轮验证无付款 | `stop` 判 `EXIT`，列出 6 条替代路径，禁止继续凭意志追加投入 |
| 命中立即停止条件 | `EXIT_NOW`，直接压到 `INSUFFICIENT` 并提示保护健康与家庭现金流 |
| 无交易数据 | `NO_DATA`，不阻塞进入第二层，标注「待回填」 |
| 有收入无工时 | `INCOMPLETE`，**拒答**，不给单位经济数字 |
| 用户描述含伪验证信号 | `BLOCKED`，当场替换为可验证指标再问 |
| 结论为 `LOCKED` | 报告仍生成，但宿主不得同时展示高分作为安慰 |
| 用户要求"给我个好结论" | 拒绝。引擎没有这个分支，宿主也不得手工上调任何分数或档位 |
| 单轮问题数超过 3 个 | 拆到下一轮。同维度自评题可 4 项一轮；需回忆凭证的开放题一轮最多 2 个（见 4.0） |
| 用户要求"一次把全部问题给我" | 拒绝（无快车道）。按批推进；用户自己一轮多答几条可以，但宿主不得主动提供这一选项 |
| 用户答"没有 / 不知道 / 跳过" | 立刻记空并标注，**不追问不说教**，直接进下一批；缺的项进修复清单 |
| 宿主想复述用户没说过的证据 | 禁止。复述只能引用用户本轮原话，且必须显式确认；不确认即按未评估封顶 2 分 |
| 平台记忆 / 历史会话里已有现成项目 | 不得直接当作候选方向或证据；先问「这个你还在做吗、愿意拿它来验证吗」，确认后才可用 |
| 第一层未过就跑 `modes` | 拒绝（`BLOCKED_ORDER`，rc=4）；没有合格方向时 `direct` 必须先跑 |
| 只给方向名、没给收入来源 | `NEEDS_INPUT`，回问"你的钱主要从哪来"，不硬套模式 |
| 关键词匹配出现多个相近候选 | 标 `ambiguous`，请用户确认后再锁指标 |
| 第一层未通过但要看板 | 看板照出，但顶部明示"不构成扩大投入依据"，并回显第一层缺口 |
| 混合模式（H 类） | 第一模式取混合包 `P29`，同时给出各部分叠加包的指标 |
| 用户想让看板数字"好看一点" | 拒绝。指标数值必须用户原样填写，缺失一律标「待填」 |

## 九、合规红线

1. 不做鼓励性表达。任何输出不得出现"保证月入 X 万""成功率 X%"类表述；最高档结论是"可进入第二层自查"。
2. 不替用户造证据。访谈原话、付款记录、工时必须用户原样输入；缺失一律按无证据封顶。这是本 skill 的底线，一旦替用户"合理推测"了付款证据，它就变成自我说服机器。
3. 不压缩时间维度。14 天冲刺、两轮 ×30 天验证是真实时间，不在一次会话里假装走完。
4. 不把热度当权重。方向评分卡 9 项里没有热度项，不得新增。
5. 合规判断不越界。涉及资质、合同、税务、数据、知识产权及受监管行业时，只输出"需专业人士核验"，不给结论。
6. 对外交付时脱敏。报告可能作为交付物或课程素材，`report` 默认按 `audience: external` 脱敏，隐藏姓名与现金绝对值，只保留比例指标（毛利率、时薪达标与否）。自用版才用 `--no-mask`。
7. 不替用户决定方向去留。 `stop` 只给出停止/转向的触发条件与替代路径，是否执行由用户决定；报告不得写成"你应该辞职"或"你应该放弃"。
8. "被压制的档位"不可隐藏。只要存在阻断项，报告必须列出它把结论压到哪一档。宿主不得删改这张表来让结论看起来更好。
9. 模式目录不得反用于选题。顺序写死在引擎里：先由第四章四类证据生成候选方向，再用模式地图归类，最后匹配指标。`modes` 在候选方向产生前直接拒绝，退出码 4。
10. 不替用户填指标。第二层的五槽位、周看板、记录格式全部由用户填写；缺失标「待填」。宿主可以提供候选池，但不得代选或代填数值。
11. 不引用历史会话与平台记忆。候选方向与证据只能来自当事人**本次亲口给出**的事实。宿主不得把平台记忆画像、历史会话里的项目当作候选方向或现成证据，那也是「替用户造证据」的一种形式，而且当事人可能早已放弃该项目、或不愿拿它来验证。确有必要提及某个旧项目时，必须先问「这个你还在做吗？愿意拿它来验证吗？」，得到确认后才可作为输入。
12. 不把提问量变成压力。单轮最多 3 问，答"没有"是合法且完整的回答。宿主不得用"这批答完才能继续""答不全结论就不准"之类的话施压，也不得因为用户答"没有"而反复劝说。

## 十、已实现 / 后续可做

两层闸门已全部实现（v0.6.1）。判据本身未变，v0.6.1 只把**宿主的提问节奏**从"按闸门分段"收紧成"按批推进"，并新增 `DC-02` 回归用例锁住这套节奏。改动点：

- 新增 4.0 节七条硬约束（单轮 ≤3 问 / 一题一行 / 无快车道 / "没有"是完整答案 / 进度句 / 复述确认 / 接住超前作答），与批次总表。
- 第一层由"一条消息铺完一道闸门"改为 25–27 轮、每轮 2–3 问；第二层 5 轮。
- 硬红线、软风险、首 3 单改「清单式」1 问；MSO 19 字段每轮 ≤3 个。

后续可选，均不改变现有判据：

- `state` 与第二层的联动再细化：目前 `infer_stage` 已能识别 `layer2_modes` / `layer2_metrics` / `layer2_board` 三个阶段。
- `assets/modes.json` 的模式关键词可继续补充行业同义词，提高归类命中率；但加词不得放松两道拒绝（顺序拒绝、收入来源缺失拒绝）。
- 不计划做：automation / 定时提醒 / 后台任务。时间维度由用户自己的节奏承担，引擎只做判据。
